/**
 * Carimbo de versão injetado pelo Vite (`define` em `vite.config.ts`).
 *
 * Declarado aqui, e não em `vite/client`, porque é constante do projeto e não
 * do Vite: quem lê `sw.ts` precisa achar de onde ela vem.
 */
declare const __VERSAO_DO_BUILD__: string;
